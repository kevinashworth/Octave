<span class="heading">
{{comment.result.user.displayName}}
left a new comment on
<a href="{{comment.result.pageUrl}}" class="action-link">{{comment.result.pageUrl}}</a>:
</span>
<br/><br/>

<div class="comment-body">
{{{comment.result.htmlBody}}}
</div>
<br>

<a href="{{comment.result.pageUrl}}" class="action-link">Reply</a><br/><br/>
