// similar to Dropdown.Item but on click there's no toggle nor color flash

import styled from 'styled-components'

const DropdownItem = styled.div`
  padding: 10px 20px;
  white-space: nowrap;
`

export default DropdownItem
